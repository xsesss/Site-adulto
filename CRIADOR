<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <meta name="theme-color" content="#0a0a0a" />
    <title>Xsess · Studio</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700;800;900&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            background: #0a0a0a;
            font-family: 'Inter', sans-serif;
            color: #f0f0f0;
            min-height: 100vh;
            padding: 24px;
        }
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: #0a0a0a;
        }
        ::-webkit-scrollbar-thumb {
            background: #cc0033;
            border-radius: 4px;
        }

        .container {
            max-width: 1100px;
            margin: 0 auto;
            background: #111;
            border: 1px solid #1a1a1a;
            border-radius: 20px;
            padding: 40px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 16px;
            margin-bottom: 32px;
            padding-bottom: 20px;
            border-bottom: 1px solid #1a1a1a;
        }
        .logo {
            font-size: 28px;
            font-weight: 900;
            color: #fff;
        }
        .logo span {
            color: #cc0033;
        }
        .logo small {
            display: block;
            font-size: 13px;
            font-weight: 400;
            color: #555;
        }
        .badge {
            background: #cc0033;
            padding: 6px 18px;
            border-radius: 30px;
            color: #fff;
            font-weight: 700;
            font-size: 12px;
            letter-spacing: 0.5px;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
            gap: 16px;
            margin-bottom: 32px;
        }
        .stat-card {
            background: #0a0a0a;
            border: 1px solid #1a1a1a;
            border-radius: 12px;
            padding: 16px 20px;
            text-align: center;
        }
        .stat-card .number {
            font-size: 30px;
            font-weight: 900;
            color: #cc0033;
        }
        .stat-card .label {
            color: #666;
            font-size: 13px;
            margin-top: 4px;
        }

        .form-title {
            font-size: 22px;
            font-weight: 700;
            color: #fff;
            margin-bottom: 24px;
        }
        .form-title span {
            color: #cc0033;
        }

        .form-row {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-group label {
            display: block;
            color: #888;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 6px;
        }
        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 14px 16px;
            background: #0a0a0a;
            border: 1px solid #2a2a2a;
            border-radius: 10px;
            color: #fff;
            font-size: 15px;
            font-family: 'Inter', sans-serif;
            transition: border-color 0.25s;
        }
        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            border-color: #cc0033;
            outline: none;
        }
        .form-group textarea {
            min-height: 100px;
            resize: vertical;
        }
        .form-group .help {
            color: #555;
            font-size: 12px;
            margin-top: 6px;
        }

        .upload-area {
            border: 2px dashed #2a2a2a;
            border-radius: 12px;
            padding: 40px 20px;
            text-align: center;
            transition: all 0.3s;
            cursor: pointer;
            position: relative;
            background: #0a0a0a;
        }
        .upload-area:hover {
            border-color: #cc0033;
            background: rgba(204, 0, 51, 0.04);
        }
        .upload-area .icon {
            font-size: 44px;
            margin-bottom: 10px;
            display: block;
            color: #444;
        }
        .upload-area .text {
            color: #888;
            font-size: 15px;
        }
        .upload-area .text strong {
            color: #cc0033;
        }
        .upload-area .sub {
            color: #555;
            font-size: 13px;
            margin-top: 4px;
        }
        .upload-area input[type="file"] {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            opacity: 0;
            cursor: pointer;
        }

        .upload-preview {
            display: none;
            margin-top: 16px;
            padding: 16px;
            background: #0a0a0a;
            border: 1px solid #2a2a2a;
            border-radius: 10px;
        }
        .upload-preview.active {
            display: block;
        }
        .upload-preview .preview-row {
            display: flex;
            gap: 16px;
            flex-wrap: wrap;
            align-items: center;
        }
        .upload-preview .preview-row .info {
            flex: 1;
        }
        .upload-preview .preview-row .info .name {
            color: #fff;
            font-weight: 600;
        }
        .upload-preview .preview-row .info .size {
            color: #666;
            font-size: 13px;
        }
        .upload-preview .preview-row .status {
            color: #00c853;
            font-weight: 600;
            font-size: 14px;
        }
        .upload-preview video,
        .upload-preview img {
            max-width: 200px;
            max-height: 120px;
            border-radius: 8px;
            background: #000;
            object-fit: cover;
        }

        .thumb-upload {
            display: flex;
            gap: 20px;
            flex-wrap: wrap;
            align-items: flex-start;
        }
        .thumb-upload .upload-area {
            flex: 1;
            min-height: 140px;
            padding: 20px;
        }
        .thumb-upload .thumb-preview {
            width: 200px;
            height: 112px;
            background: #0a0a0a;
            border: 1px solid #2a2a2a;
            border-radius: 10px;
            overflow: hidden;
            display: none;
            flex-shrink: 0;
        }
        .thumb-upload .thumb-preview.active {
            display: block;
        }
        .thumb-upload .thumb-preview img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .btn {
            padding: 14px 32px;
            border: none;
            border-radius: 30px;
            font-weight: 700;
            font-size: 16px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Inter', sans-serif;
            display: inline-flex;
            align-items: center;
            gap: 10px;
        }
        .btn-success {
            background: #00c853;
            color: #fff;
            box-shadow: 0 4px 20px rgba(0, 200, 83, 0.15);
        }
        .btn-success:hover {
            background: #00b34a;
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(0, 200, 83, 0.25);
        }
        .btn-secondary {
            background: transparent;
            border: 1px solid #2a2a2a;
            color: #888;
        }
        .btn-secondary:hover {
            border-color: #cc0033;
            color: #fff;
        }
        .btn-danger {
            background: #d32f2f;
            color: #fff;
        }
        .btn-danger:hover {
            background: #b71c1c;
        }

        .form-actions {
            display: flex;
            gap: 16px;
            flex-wrap: wrap;
            margin-top: 8px;
        }
        .form-actions .btn {
            flex: 1;
            justify-content: center;
            min-width: 140px;
        }

        .message {
            padding: 16px 20px;
            border-radius: 12px;
            margin-bottom: 20px;
            display: none;
            font-weight: 600;
            align-items: center;
            gap: 12px;
        }
        .message.success {
            display: flex;
            background: rgba(0, 200, 83, 0.08);
            border: 1px solid #00c853;
            color: #69f0ae;
        }
        .message.error {
            display: flex;
            background: rgba(211, 47, 47, 0.08);
            border: 1px solid #d32f2f;
            color: #ef9a9a;
        }
        .message .close-msg {
            margin-left: auto;
            cursor: pointer;
            font-size: 18px;
            opacity: 0.5;
            background: none;
            border: none;
            color: inherit;
        }
        .message .close-msg:hover {
            opacity: 1;
        }

        .preview-section {
            display: none;
            margin-top: 32px;
            padding-top: 32px;
            border-top: 1px solid #1a1a1a;
        }
        .preview-section.active {
            display: block;
        }
        .preview-card {
            background: #0a0a0a;
            border: 1px solid #1a1a1a;
            border-radius: 12px;
            overflow: hidden;
        }
        .preview-card .preview-player {
            width: 100%;
            aspect-ratio: 16/9;
            background: #000;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .preview-card .preview-player video {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }
        .preview-card .preview-info {
            padding: 20px;
        }
        .preview-card .preview-info .p-title {
            font-size: 18px;
            font-weight: 700;
            color: #fff;
        }
        .preview-card .preview-info .p-meta {
            display: flex;
            gap: 16px;
            font-size: 14px;
            color: #666;
            margin-top: 6px;
            flex-wrap: wrap;
        }
        .preview-card .preview-info .p-tags {
            display: flex;
            gap: 8px;
            margin-top: 10px;
            flex-wrap: wrap;
        }
        .preview-card .preview-info .p-tags span {
            color: #cc0033;
            font-weight: 600;
            font-size: 12px;
            text-transform: uppercase;
            border: 1px solid #2a1a1a;
            padding: 2px 14px;
            border-radius: 4px;
        }
        .preview-card .preview-info .p-desc {
            color: #888;
            font-size: 14px;
            margin-top: 12px;
            line-height: 1.6;
        }

        .video-list {
            margin-top: 32px;
            padding-top: 32px;
            border-top: 1px solid #1a1a1a;
        }
        .video-list .list-title {
            font-size: 18px;
            font-weight: 700;
            color: #fff;
            margin-bottom: 16px;
        }
        .video-list .list-title span {
            color: #cc0033;
        }
        .video-list .list-item {
            display: flex;
            align-items: center;
            gap: 16px;
            background: #0a0a0a;
            border: 1px solid #1a1a1a;
            border-radius: 10px;
            padding: 12px 16px;
            margin-bottom: 10px;
            transition: border-color 0.25s;
        }
        .video-list .list-item:hover {
            border-color: #2a2a2a;
        }
        .video-list .list-item .thumb {
            width: 80px;
            height: 45px;
            background: #1a1a1a;
            border-radius: 6px;
            overflow: hidden;
            flex-shrink: 0;
        }
        .video-list .list-item .thumb img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .video-list .list-item .info {
            flex: 1;
        }
        .video-list .list-item .info .name {
            color: #fff;
            font-weight: 600;
            font-size: 14px;
        }
        .video-list .list-item .info .meta {
            color: #666;
            font-size: 12px;
        }
        .video-list .list-item .actions {
            display: flex;
            gap: 8px;
        }
        .video-list .list-item .actions .btn-sm {
            padding: 6px 14px;
            font-size: 12px;
            font-weight: 600;
            border: 1px solid #2a2a2a;
            background: transparent;
            color: #888;
            border-radius: 6px;
            cursor: pointer;
            transition: all 0.25s;
            font-family: 'Inter', sans-serif;
        }
        .video-list .list-item .actions .btn-sm:hover {
            border-color: #cc0033;
            color: #cc0033;
        }
        .video-list .list-item .actions .btn-sm.danger:hover {
            border-color: #d32f2f;
            color: #d32f2f;
        }

        .empty-state {
            text-align: center;
            padding: 40px 20px;
            color: #555;
        }
        .empty-state .icon {
            font-size: 48px;
            margin-bottom: 12px;
            display: block;
            color: #2a2a2a;
        }

        @media (max-width: 768px) {
            .container {
                padding: 20px;
            }
            .form-row {
                grid-template-columns: 1fr;
            }
            .header {
                flex-direction: column;
                align-items: flex-start;
            }
            .form-actions {
                flex-direction: column;
            }
            .form-actions .btn {
                flex: none;
                width: 100%;
            }
            .thumb-upload {
                flex-direction: column;
            }
            .thumb-upload .thumb-preview {
                width: 100%;
                height: 150px;
            }
            .stats {
                grid-template-columns: 1fr 1fr;
            }
            .video-list .list-item {
                flex-wrap: wrap;
            }
            .video-list .list-item .actions {
                width: 100%;
                justify-content: flex-end;
            }
        }
        @media (max-width: 480px) {
            .stats {
                grid-template-columns: 1fr;
            }
            .upload-area {
                padding: 20px 16px;
            }
            .upload-area .icon {
                font-size: 32px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <div class="logo">X<span>sess</span> <small>Painel do Criador · Studio</small></div>
            <span class="badge"><i class="fas fa-crown"></i> PRO</span>
        </header>

        <div class="stats">
            <div class="stat-card"><div class="number" id="totalVideos">0</div><div class="label">Vídeos</div></div>
            <div class="stat-card"><div class="number" id="totalLikes">0</div><div class="label">Curtidas</div></div>
            <div class="stat-card"><div class="number" id="totalComments">0</div><div class="label">Comentários</div></div>
            <div class="stat-card"><div class="number" id="totalViews">0</div><div class="label">Visualizações</div></div>
        </div>

        <div class="message success" id="msgSuccess"><i class="fas fa-check-circle"></i> <span id="successText">Vídeo publicado!</span> <button class="close-msg" onclick="this.parentElement.style.display='none'"><i class="fas fa-times"></i></button></div>
        <div class="message error" id="msgError"><i class="fas fa-exclamation-circle"></i> <span id="errorText">Erro!</span> <button class="close-msg" onclick="this.parentElement.style.display='none'"><i class="fas fa-times"></i></button></div>

        <h2 class="form-title"><i class="fas fa-upload" style="color:#cc0033;"></i> <span>Publicar</span> Novo Vídeo</h2>
        <form id="videoForm">

            <div class="form-group">
                <label><i class="fas fa-video"></i> Arquivo do Vídeo *</label>
                <div class="upload-area" id="videoUploadArea">
                    <span class="icon"><i class="fas fa-cloud-upload-alt"></i></span>
                    <div class="text"><strong>Clique ou arraste</strong> o arquivo de vídeo</div>
                    <div class="sub">MP4, WebM, MOV · Máx. 500MB</div>
                    <input type="file" id="videoFile" accept="video/*" required />
                </div>
                <div class="upload-preview" id="videoPreviewUpload">
                    <div class="preview-row">
                        <video id="previewVideo" controls style="width:200px;height:112px;object-fit:cover;border-radius:8px;background:#000;"></video>
                        <div class="info"><div class="name" id="videoFileName">video.mp4</div><div class="size" id="videoFileSize">0 MB</div></div>
                        <div class="status"><i class="fas fa-check-circle"></i> Carregado</div>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label><i class="fas fa-image"></i> Thumbnail</label>
                <div class="thumb-upload">
                    <div class="upload-area" id="thumbUploadArea">
                        <span class="icon"><i class="fas fa-cloud-upload-alt"></i></span>
                        <div class="text"><strong>Clique ou arraste</strong> a imagem de capa</div>
                        <div class="sub">JPG, PNG · 16:9</div>
                        <input type="file" id="thumbFile" accept="image/*" />
                    </div>
                    <div class="thumb-preview" id="thumbPreview"><img id="thumbPreviewImg" src="" alt="Thumbnail" /></div>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group"><label for="videoTitle"><i class="fas fa-tag"></i> Título *</label><input type="text" id="videoTitle" placeholder="Ex: Lançamento Exclusivo" required /></div>
                <div class="form-group"><label for="videoCategory"><i class="fas fa-folder"></i> Categoria</label>
                    <select id="videoCategory">
                        <option value="">Selecione...</option>
                        <option value="Exclusivo">🔥 Exclusivo</option>
                        <option value="4K">📺 4K</option>
                        <option value="Novo">🆕 Novo</option>
                        <option value="Em Alta">📈 Em Alta</option>
                        <option value="Premium">💎 Premium</option>
                    </select>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group"><label for="videoTags"><i class="fas fa-hashtag"></i> Tags</label><input type="text" id="videoTags" placeholder="4K, Exclusivo, Novo" /></div>
                <div class="form-group"><label for="videoDuration"><i class="fas fa-clock"></i> Duração</label><input type="text" id="videoDuration" placeholder="12:30" /></div>
            </div>

            <div class="form-group"><label for="videoDescription"><i class="fas fa-align-left"></i> Descrição</label><textarea id="videoDescription" placeholder="Descreva o conteúdo..."></textarea></div>

            <div class="form-actions">
                <button type="submit" class="btn btn-success"><i class="fas fa-upload"></i> Publicar</button>
                <button type="button" class="btn btn-secondary" id="btnPreview"><i class="fas fa-eye"></i> Pré-visualizar</button>
                <button type="button" class="btn btn-secondary" id="btnClear"><i class="fas fa-eraser"></i> Limpar</button>
            </div>
        </form>

        <div class="preview-section" id="previewSection">
            <h3 style="color:#fff;font-size:18px;margin-bottom:16px;"><i class="fas fa-eye" style="color:#cc0033;"></i> Pré-visualização</h3>
            <div class="preview-card">
                <div class="preview-player"><video id="previewPlayer" controls></video></div>
                <div class="preview-info">
                    <div class="p-title" id="previewTitle">Título</div>
                    <div class="p-meta"><span id="previewDate">00/00/0000</span><span id="previewDuration">Duração: --:--</span></div>
                    <div class="p-tags" id="previewTags"></div>
                    <div class="p-desc" id="previewDesc">Descrição</div>
                </div>
            </div>
        </div>

        <div class="video-list">
            <div class="list-title"><i class="fas fa-film" style="color:#cc0033;"></i> <span>Meus</span> Vídeos</div>
            <div id="videoListContainer"></div>
        </div>
    </div>

    <script>
        const DB = { get: (k, d) => { try { const data = localStorage.getItem('xsess_' + k); return data ? JSON.parse(
                    data) : d; } catch { return d; } }, set: (k, v) => { localStorage.setItem('xsess_' + k, JSON.stringify(
                    v)); } };

        let videos = DB.get('videos', []);
        let likes = DB.get('likes', {});
        let comments = DB.get('comments', {});
        let videoFileData = null;
        let thumbFileData = null;

        const $ = (id) => document.getElementById(id);

        function generateId() { return Date.now().toString(36) + Math.random().toString(36).substr(2, 6); }

        function formatDate() { const now = new Date(); return now.toLocaleDateString('pt-BR') + ' ' + now
            .toLocaleTimeString('pt-BR', { hour: '2-digit', minute: '2-digit' }); }

        function formatFileSize(bytes) { if (bytes < 1024) return bytes + ' B'; if (bytes < 1048576) return (bytes / 1024)
                .toFixed(1) + ' KB'; return (bytes / 1048576).toFixed(1) + ' MB'; }

        function showSuccess(msg) { const el = $('msgSuccess'); $('successText').textContent = msg;
            el.style.display = 'flex'; $('msgError').style.display = 'none'; setTimeout(() => el.style.display = 'none',
                4000); }

        function showError(msg) { const el = $('msgError'); $('errorText').textContent = msg;
            el.style.display = 'flex'; $('msgSuccess').style.display = 'none'; setTimeout(() => el.style.display = 'none',
                4000); }

        const videoUploadArea = $('videoUploadArea');
        const videoFileInput = $('videoFile');
        const videoPreviewUpload = $('videoPreviewUpload');
        const previewVideo = $('previewVideo');
        const videoFileName = $('videoFileName');
        const videoFileSize = $('videoFileSize');

        videoUploadArea.addEventListener('dragover', (e) => { e.preventDefault();
            videoUploadArea.classList.add('dragover'); });
        videoUploadArea.addEventListener('dragleave', () => { videoUploadArea.classList.remove('dragover'); });
        videoUploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            videoUploadArea.classList.remove('dragover');
            if (e.dataTransfer.files.length > 0) handleVideoFile(e.dataTransfer.files[0]);
        });
        videoFileInput.addEventListener('change', (e) => { if (e.target.files.length > 0) handleVideoFile(e.target.files[
                0]); });

        function handleVideoFile(file) {
            const validTypes = ['video/mp4', 'video/webm', 'video/quicktime', 'video/x-msvideo'];
            if (!validTypes.includes(file.type) && !file.name.match(/\.(mp4|webm|mov|avi|mkv)$/i)) { showError(
                    'Formato não suportado.'); return; }
            if (file.size > 500 * 1024 * 1024) { showError('Máximo 500MB.'); return; }
            videoFileData = file;
            videoFileName.textContent = file.name;
            videoFileSize.textContent = formatFileSize(file.size);
            videoPreviewUpload.classList.add('active');
            const reader = new FileReader();
            reader.onload = (e) => {
                previewVideo.src = e.target.result;
                previewVideo.load();
                previewVideo.onloadedmetadata = () => {
                    const dur = previewVideo.duration;
                    if (dur && !isNaN(dur) && dur > 0) {
                        const mins = Math.floor(dur / 60);
                        const secs = Math.floor(dur % 60);
                        $('videoDuration').value = String(mins).padStart(2, '0') + ':' + String(secs).padStart(2,
                            '0');
                    }
                };
            };
            reader.readAsDataURL(file);
            const previewPlayer = $('previewPlayer');
            if (previewPlayer) { previewPlayer.src = URL.createObjectURL(file);
                previewPlayer.load(); }
        }

        const thumbUploadArea = $('thumbUploadArea');
        const thumbFileInput = $('thumbFile');
        const thumbPreview = $('thumbPreview');
        const thumbPreviewImg = $('thumbPreviewImg');

        thumbUploadArea.addEventListener('dragover', (e) => { e.preventDefault();
            thumbUploadArea.classList.add('dragover'); });
        thumbUploadArea.addEventListener('dragleave', () => { thumbUploadArea.classList.remove('dragover'); });
        thumbUploadArea.addEventListener('drop', (e) => {
            e.preventDefault();
            thumbUploadArea.classList.remove('dragover');
            if (e.dataTransfer.files.length > 0) handleThumbFile(e.dataTransfer.files[0]);
        });
        thumbFileInput.addEventListener('change', (e) => { if (e.target.files.length > 0) handleThumbFile(e.target.files[
                0]); });

        function handleThumbFile(file) {
            if (!file.type.startsWith('image/')) { showError('Envie uma imagem JPG ou PNG.'); return; }
            thumbFileData = file;
            const reader = new FileReader();
            reader.onload = (e) => { thumbPreviewImg.src = e.target.result;
                thumbPreview.classList.add('active'); };
            reader.readAsDataURL(file);
        }

        $('btnPreview').addEventListener('click', () => {
            const title = $('videoTitle').value.trim();
            if (!title) { showError('Digite um título.'); return; }
            if (!videoFileData) { showError('Envie um vídeo.'); return; }
            const category = $('videoCategory').value;
            const tags = $('videoTags').value.trim();
            const duration = $('videoDuration').value.trim();
            const desc = $('videoDescription').value.trim();
            const section = $('previewSection');
            section.classList.add('active');
            $('previewTitle').textContent = title;
            $('previewDate').textContent = formatDate();
            $('previewDuration').textContent = duration ? `Duração: ${duration}` : 'Duração: --:--';
            $('previewDesc').textContent = desc || 'Sem descrição.';
            const tagsContainer = $('previewTags');
            tagsContainer.innerHTML = '';
            if (category) { const span = document.createElement('span');
                span.textContent = category;
                tagsContainer.appendChild(span); }
            if (tags) { tags.split(',').forEach(tag => { const span = document.createElement('span');
                    span.textContent = tag.trim();
                    tagsContainer.appendChild(span); }); }
            section.scrollIntoView({ behavior: 'smooth', block: 'start' });
        });

        $('videoForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const title = $('videoTitle').value.trim();
            const category = $('videoCategory').value;
            const tags = $('videoTags').value.trim();
            const duration = $('videoDuration').value.trim();
            const desc = $('videoDescription').value.trim();
            if (!title) { showError('Digite o título.'); return; }
            if (!videoFileData) { showError('Selecione um vídeo.'); return; }

            const video = {
                id: generateId(),
                title: title,
                category: category,
                tags: tags ? tags.split(',').map(t => t.trim()).filter(t => t) : [],
                duration: duration || '--:--',
                description: desc || '',
                date: formatDate(),
                author: 'Xsess Creator',
                views: 0,
                thumbnail: thumbFileData ? null : 'https://via.placeholder.com/640x360/1a1a1a/cc0033?text=Sem+Imagem',
                link: URL.createObjectURL(videoFileData)
            };

            if (thumbFileData) {
                const reader = new FileReader();
                reader.onload = (e) => { video.thumbnail = e.target.result;
                    saveVideo(video); };
                reader.readAsDataURL(thumbFileData);
            } else { saveVideo(video); }
        });

        function saveVideo(video) {
            videos.unshift(video);
            DB.set('videos', videos);
            renderList();
            updateStats();
            showSuccess('✅ Vídeo "' + video.title + '" publicado!');
            $('videoForm').reset();
            videoFileData = null;
            thumbFileData = null;
            videoPreviewUpload.classList.remove('active');
            thumbPreview.classList.remove('active');
            previewVideo.src = '';
            const previewPlayer = $('previewPlayer');
            if (previewPlayer) previewPlayer.src = '';
            $('previewSection').classList.remove('active');
        }

        function renderList() {
            const container = $('videoListContainer');
            if (videos.length === 0) {
                container.innerHTML =
                    `<div class="empty-state"><span class="icon"><i class="fas fa-film"></i></span><div>Nenhum vídeo publicado</div><div style="color:#444;font-size:14px;margin-top:4px;">Publique seu primeiro vídeo</div></div>`;
                return;
            }
            container.innerHTML = videos.map((v, index) => `
                    <div class="list-item">
                        <div class="thumb"><img src="${v.thumbnail || 'https://via.placeholder.com/80x45/1a1a1a/cc0033?text=Video'}" alt="${v.title}" /></div>
                        <div class="info"><div class="name">${v.title}</div><div class="meta">${v.date} · ${v.duration} · ${v.tags.length > 0 ? v.tags.join(', ') : 'Sem tags'}</div></div>
                        <div class="actions">
                            <button class="btn-sm" onclick="window.open('https://xsesss.github.io/Si/','_blank')"><i class="fas fa-eye"></i> Ver</button>
                            <button class="btn-sm danger" onclick="deleteVideo('${v.id}')"><i class="fas fa-trash"></i></button>
                        </div>
                    </div>
                `).join('');
        }

        function deleteVideo(id) {
            if (!confirm('Excluir este vídeo?')) return;
            videos = videos.filter(v => v.id !== id);
            DB.set('videos', videos);
            renderList();
            updateStats();
            showSuccess('🗑️ Vídeo removido.');
        }

        function updateStats() {
            $('totalVideos').textContent = videos.length;
            $('totalLikes').textContent = Object.values(likes).reduce((a, b) => a + b, 0);
            $('totalComments').textContent = Object.values(comments).reduce((a, b) => a + b.length, 0);
            $('totalViews').textContent = videos.reduce((acc, v) => acc + (v.views || 0), 0);
        }

        $('btnClear').addEventListener('click', () => {
            $('videoForm').reset();
            videoFileData = null;
            thumbFileData = null;
            videoPreviewUpload.classList.remove('active');
            thumbPreview.classList.remove('active');
            previewVideo.src = '';
            const previewPlayer = $('previewPlayer');
            if (previewPlayer) previewPlayer.src = '';
            $('previewSection').classList.remove('active');
        });

        renderList();
        updateStats();
    </script>
</body>
</html>
